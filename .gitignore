*.mov
*.mp4
*.avi
*.pt
.DS_Store
.ipynb_checkpoints/

# Claude Code local config — never committed
.claude/
*.code-workspace

# Trained weights — drop best.pt from Colab into weights/, never committed
weights/*
!weights/.gitkeep

# All video files live in data/cache/ — large, never committed
data/cache/

# Pipeline run outputs — never committed
outputs/

