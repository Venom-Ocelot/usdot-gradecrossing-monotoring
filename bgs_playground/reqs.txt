# Combined dependencies for tracking + training pipeline.
#
# GPU-only: PyTorch must come from the CUDA wheel index. Do not install
# unqualified torch/torchvision wheels for this project.
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Tracking (DeepSORT's mobilenet embedder needs torch + torchvision, above)
deep-sort-realtime==1.3.2

# Training pipeline
transformers>=4.35.0
ultralytics>=8.3.0
scikit-learn>=1.3.0
Pillow>=10.0.0
# Shared
numpy==2.4.6
opencv-python==4.13.0.92
