numpy==2.4.6
opencv-python==4.13.0.92
deep-sort-realtime==1.3.2

# DeepSORT's mobilenet embedder needs torch + torchvision.
# CPU builds (no CUDA): install with the PyTorch CPU index, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
torchvision==0.27.1
